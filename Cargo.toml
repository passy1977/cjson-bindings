[package]
name = "cjson-bindings"
version = "0.6.6"
authors = ["Antonio Salsi <passy.linux@zresa.it>"]
edition = "2024"
build = "build.rs"
license = "LGPL-2.1-or-later"
readme = "README.md"
repository = "https://github.com/HiHappyGarden/cjson-bindings"
documentation = "https://docs.rs/cjson-bindings"
homepage = "https://github.com/HiHappyGarden/cjson-bindings"
description = "Safe Rust bindings for the cJSON library - a lightweight JSON parser in C with support for JSON Pointer (RFC6901), JSON Patch (RFC6902), and JSON Merge Patch (RFC7386)"
keywords = ["json", "cjson", "no_std", "embedded", "parser"]
categories = ["no-std", "parsing", "embedded", "api-bindings"]
autobenches = false
autotests = false

# Metadata for docs.rs
[package.metadata.docs.rs]
features = ["std"]
default-target = "x86_64-unknown-linux-gnu"
targets = ["x86_64-unknown-linux-gnu", "aarch64-unknown-linux-gnu", "armv7-unknown-linux-gnueabihf", "thumbv8m.main-none-eabi"]

[lib]
name = "cjson_binding"
crate-type = ["staticlib", "rlib"]

[features]
default = []
std = []
disable_panic = []
osal_rs = ["dep:osal-rs", "dep:osal-rs-serde", "disable_panic"]

[dependencies]
osal-rs = { version = "1", path = "../osal-rs/osal-rs", features = ["freertos", "serde"], optional = true }
osal-rs-serde = { version = "1", path = "../osal-rs/osal-rs-serde", features = ["derive"], optional = true }

[build-dependencies]
pkg-config = "0.3"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"